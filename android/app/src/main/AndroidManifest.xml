<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:label="Lumi"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher">
        <!-- ── Meta App Events ────────────────────────────────────────────
             The Facebook SDK auto-initializes at process start via its own
             ContentProvider and crashes on a missing/empty App ID, so this
             can't be left blank the way YANDEX_MAPKIT_KEY is. MetaService
             itself stays a no-op until META_APP_ID/META_CLIENT_TOKEN are also
             passed via --dart-define. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="1433809481948376"/>
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="8a66838afbeed59eef2fd54b76e57b1d"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Custom URL scheme: lumi://class/:id -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="lumi" android:host="class"/>
            </intent-filter>
            <!-- App Links: opens /share/class/* directly in the app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="mobile-api.lumipass.uz"
                    android:pathPrefix="/share/class/"/>
            </intent-filter>
            <!-- AppsFlyer OneLink. BOTH hosts are claimed: campaigns use the
                 branded domain, but AppsFlyer also serves every link from the
                 raw OneLink subdomain, and its own setup snippet asks for that
                 one. Whichever the click came from has to resolve in-app.
                 No pathPrefix — the path starts with the template id (JBWe),
                 which changes if a second template is ever added.
                 Verification needs AppsFlyer to serve assetlinks.json for each
                 host — see docs/APPSFLYER.md §Deep links. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="link.lumipass.uz"/>
                <data android:scheme="http" android:host="link.lumipass.uz"/>
                <data android:scheme="https" android:host="lumipass.onelink.me"/>
                <data android:scheme="http" android:host="lumipass.onelink.me"/>
            </intent-filter>
        </activity>
        <!-- Default FCM notification channel for Android 8+ -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="lumi_channel" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Maps apps the "build route" sheet hands the venue off to. Android
             11+ hides packages we haven't declared, which would make
             canLaunchUrl report them missing. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yandexmaps"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="yandexnavi"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
    </queries>
</manifest>
